export declare const User: {
    getUser(inaConst: {
        secret_key?: string;
        mode: string;
    }): Promise<any>;
};
export declare function getUsers(inaConst: {
    secret_key?: string;
    mode: string;
}): Promise<any>;
